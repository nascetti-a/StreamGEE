# 🌍 Streamlit + Google Earth Engine Demo

Minimal demo app showing how to initialize **Google Earth Engine (GEE)** inside a Streamlit app.

---

## 🚀 Run locally

1. Clone repo & install deps:
   ```bash
   pip install -r requirements.txt